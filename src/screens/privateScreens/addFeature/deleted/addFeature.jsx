// import { useNavigate } from "react-router-dom";
// import useFeatureForm from "./useFeatureForm";
// import FeatureForm from "./FeatureForm";
// import withAuthorization from "./withAuthorization";

// const AddFeature = () => {
//   const navigate = useNavigate();
//   const { users, formData, handleChange, handleSubmit } = useFeatureForm();

//   return (
//     <div className="flex flex-col items-center justify-center min-h-screen p-6">
//       <h2 className="text-2xl font-bold mb-6">Add Feature</h2>
//       <FeatureForm users={users} formData={formData} handleChange={handleChange} handleSubmit={handleSubmit} />
//     </div>
//   );
// };

// export default withAuthorization(AddFeature);